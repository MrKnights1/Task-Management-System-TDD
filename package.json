{
  "name": "testjuhitudarendus",
  "module": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "db:migrate": "prisma migrate dev",
    "db:reset": "prisma migrate reset",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "start": "bun index.js",
    "dev": "bun --hot index.js",
    "setup": "cp -n .env.example .env 2>/dev/null || true && bun install && bunx prisma generate && bunx prisma migrate deploy && bun start"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@prisma/client": "^6.17.1",
    "jest": "^30.2.0",
    "prisma": "^6.17.1"
  }
}
